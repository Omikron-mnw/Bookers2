
<% if book.errors.any? %>
<div id="error_explanation">
  <span><%= pluralize(book.errors.count, "error") %></span>
  <span>
    <%= book.errors.full_messages.each do |message| %>
      <span><%= message %></span>
  <% end %>
  </span>
</div>
<% end %>
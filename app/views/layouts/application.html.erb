<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/*******.js" crossorigin="anonymous"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body calss="d-flex flex-column vh-100">
    <header calss="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand p-3" herf="/">
            <span>Bookers</span>
          </a>
          <button class="navbar-toggler" type="button" daata-toggle="collapse" data-target="#navbarNavDropdown" aria-control="navbarNavDropdown" aria-expanded="false" aria="Toggle navigation">
            <span class="navbar-toggle-icon"></span>
          </button>
          <div calss="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <i class="fas fa-home"></i><%= link_to "Home", user_path(current_user.id) %>
              </li>
              <li class="nav-item">
                <i class="fas fa-users"></i><%= link_to "Users", users_path %>   <!-- users_path? -->
              </li>
              <li class="nav-item">
                <i class="fas fa-book-open"></i><%= link_to "Books", books_path %>
              </li>
              <li class="nav-item">
                <i class="fas fa-sign-out-alt"></i><%= link_to "logout", destroy_user_session_path, method: :delete %>
              </li>
            <% else %>
              <li class="nav-item">
                <i class="fas fa-home"></i><%= link_to "Home", root_path %>
              </li>
              <li class="nav-item">
                <i class="fas fa-link"></i><%= link_to "About", homes_about_path %>
              </li>
              <li class="nav-item">
                <i class="fas fa-user-plus"></i><%= link_to "Sign up", new_user_registration_path %>
              </li>
              <li class="nav-item">
                <i class="fas fa-sign-out-alt"></i><%= link_to "login", new_user_session_path %>
              </li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <!--% if user_signed_in? %>-->
    <main class="mb-auto">
      <div class="sidebar">
        <div class="container px-5 px-sm-0">
          <!--%= render 'users/sidebar' %>-->
          <!--%= render 'books/sidebar' %>-->
        <!--<p id="notice"></p>-->
        <!--<div class="container">-->
        <!--  <div class="row">-->
        <!--    <div>-->
               <!--render '' -->
        <!--    </div>-->
        <!--    <div>-->
               <!--render '' -->
        <!--    </div>-->
        <!--  </div>-->
        <!--</div>-->
          </div>
        </div>
    <!--% else %>-->
    <!--% end %>-->
    <%= yield %>
    </main>
    <footer class="bg-light text-dark pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-8">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>

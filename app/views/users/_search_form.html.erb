
<!--%= form_tag(search_path, :method => 'get') do %>-->
<!--  %= select_tag(:option, options_for_select([["user", "1"],["book","2"]])) %>-->
<!--  %= select_tag(:choice, options_for_select([["部分一致", "1"],["後方一致", "2"],["前方一致", "3"],["完全一致", "4"]])) %>-->
<!--  %= text_field_tag :search %>-->
<!--  %= submit_tag 'Search', :name => nil %>-->
<!--% end %>-->

<%= form_with url: search_path, :method => 'get', local: true do  |f| %>
  <%= f.select(:option, options_for_select([["user", "1"],["book","2"]])) %>
  <%= f.select(:choice, options_for_select([["部分一致", "1"],["後方一致", "2"],["前方一致", "3"],["完全一致", "4"]])) %>
  <%= f.text_field :search %>
  <%= f.submit "Search", :name => nil %>
<% end %>

